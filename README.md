# Conditional_C_RNN_GAN

```
python data/scraip.py

sudo nvidia-docker build -t {image name} --no-cache ./
sudo nvidia-docker run -it -t --name {container name} {image name}
sudo docker exec -it {container name} /bin/bash

cd ~
pyenv install 3.5-dev
pyenv global 3.5-dev
pip install -r requirements.txt

```
